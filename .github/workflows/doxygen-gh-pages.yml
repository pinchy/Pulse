name: Doxygen GitHub Pages Deploy Action

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: .github/workflows/doxygen-action.yml
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}